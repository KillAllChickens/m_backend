package routes

import "github.com/gofiber/fiber/v3"

func StreamingRoutes(app *fiber.App) {
    // streamGroup := app.Group("/api")

    // streamGroup.Get("/proxy/:fid", func(c fiber.Ctx) error {
    // 	// return c.SendStream()
    // })
}
